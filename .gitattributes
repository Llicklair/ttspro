# LF everywhere, on every OS: the pre-commit is a sh script and the fixtures
# are compared byte-for-byte between Python and node.
* text=auto eol=lf
*.png binary
*.wav binary
*.flac binary
*.onnx binary
*.pt binary
