"""Training loop, losses and monotonic alignment search.

Python-only, GPU-only, never exported. Nothing under ``ttspro.model``,
``ttspro.export`` or ``ttspro.frontend`` may import this package (rule 7).
"""
